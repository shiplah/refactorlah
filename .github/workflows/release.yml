name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

defaults:
  run:
    shell: bash

jobs:
  test:
    name: Test ${{ matrix.target }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: darwin/arm64
            runner: macos-15
          - target: linux/arm64
            runner: ubuntu-24.04-arm
          - target: windows/amd64
            runner: windows-2025

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Run tests
        run: bin/test.sh

  build:
    name: Build ${{ matrix.target }}
    needs: test
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: darwin/arm64
            slug: darwin-arm64
            runner: macos-15
            archive: tar.gz
          - target: linux/arm64
            slug: linux-arm64
            runner: ubuntu-24.04-arm
            archive: tar.gz
          - target: windows/amd64
            slug: windows-amd64
            runner: windows-2025
            archive: zip

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Build release package
        run: |
          set -eu

          if [ "${GITHUB_REF_TYPE:-}" = "tag" ]; then
            version="$GITHUB_REF_NAME"
          else
            version="dev-${GITHUB_SHA}"
          fi

          export REFACTORLAH_VERSION="$version"
          export REFACTORLAH_COMMIT="${GITHUB_SHA}"
          export REFACTORLAH_BUILD_DATE="$(date -u '+%Y-%m-%dT%H:%M:%SZ')"
          export REFACTORLAH_DISTRIBUTION="github-release"

          bin/build.sh --no-test --target "${{ matrix.target }}"

      - name: Archive release package
        id: package
        run: |
          set -eu

          package="refactorlah_${{ matrix.slug }}"
          archive="$package.${{ matrix.archive }}"

          cd build/dist
          if [ "${{ matrix.archive }}" = "zip" ]; then
            powershell -NoProfile -Command "Compress-Archive -Path '$package' -DestinationPath '$archive' -Force"
          else
            tar -czf "$archive" "$package"
          fi

          printf 'archive=%s\n' "build/dist/$archive" >> "$GITHUB_OUTPUT"

      - name: Upload release package
        uses: actions/upload-artifact@v4
        with:
          name: refactorlah-${{ matrix.slug }}
          path: ${{ steps.package.outputs.archive }}
          if-no-files-found: error

  publish:
    name: Publish GitHub release
    needs: build
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    environment: release
    steps:
      - name: Download release packages
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Create checksums
        run: |
          set -eu

          cd dist
          sha256sum * > refactorlah_checksums.txt

      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -eu

          if gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1; then
            gh release upload "$GITHUB_REF_NAME" dist/* --clobber
          else
            gh release create "$GITHUB_REF_NAME" dist/* \
              --title "$GITHUB_REF_NAME" \
              --generate-notes
          fi
