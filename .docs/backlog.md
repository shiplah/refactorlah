# Backlog

These are known follow-ups that should not be lost while the adapters mature.

## Static Imports

- Decide whether `staticimports` should stay shared or become a first-class asset adapter.
- Expand static import coverage for JavaScript, TypeScript, CSS, and framework entrypoint patterns before adding more rewrite categories.

## PHP Maybe Later

- Add Laravel/Blade support as Laravel-specific coverage, not generic PHP behaviour.
- Rewrite PHP group `use` statements when the replacement can be proven safe.

## Python Maybe Later

- Add Jinja template support as framework/template coverage, not generic Python module behaviour.
- Add Django template support with Django-specific conventions modelled separately from generic Python module moves.
